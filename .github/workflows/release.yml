name: Release Obsidian plugin

on:
  push:
    tags:
      - '*'

# Sans ceci, beaucoup de dépôts ont le GITHUB_TOKEN en lecture seule par défaut
# et `gh release create` échoue sans créer de release.
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          # Node 18 : échec moteur (ex. selenium-webdriver >= 4.44 exige >= 20) si le lockfile n’est pas appliqué.
          node-version: '20.x'
          cache: 'yarn'

      - name: Build plugin
        run: |
          npm install -g yarn@1.22.22
          yarn install --frozen-lockfile
          yarn build

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"

          gh release create "$tag" \
            --title="$tag" \
            --notes-file="release-notes.md" \
            main.js manifest.json styles.css
